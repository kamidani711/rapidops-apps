# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.class

# Operating System Files
.DS_Store
Thumbs.db
Desktop.ini

# Direct APK downloads are too large for a normal git push (GitHub's 100MB
# hard limit) and are no longer linked from any page anyway — landing pages
# link to Google Play instead. Keep locally, don't commit.
*.apk
